[general]
enabled=yes
pretty=yes

[voip-agent]
type=user
read_only=no
; SET A STRONG SECRET. The agent refuses to start if ARI_PASSWORD is unset or
; "changeme" (agent/config.py validator). This value must match .env ARI_PASSWORD.
password=CHANGE_ME_STRONG_SECRET
